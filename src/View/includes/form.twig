<div class="form-section">
  <p class="title">{{ formData.title }}</p>
  <p>{{ formData.description }}</p>
  <form method="post"{% if formData.hasUpload %} enctype="multipart/form-data"{% endif %}>
    {%- for field in formData.fields %}
    <div class="field">
      {% if field.template %}
      {% include(field.template) with { property: field } %}
      {% endif %}
    </div>
    {%- endfor %}
    <div class="field">
      <div class="control">
        <div class="buttons">
        <button class="button is-link">{{ formData.submitText }}</button>
        <a class="button" href="javascript:history.back()">Go Back</a>
        </div>
      </div>
    </div>
  </form>
</div>